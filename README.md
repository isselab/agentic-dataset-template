# agentic-feature-traced-dataset-template
Template for spinning up a new dataset repo (agentic/vibe-coded codebase + embedded traceability links). "Use this template" → new repo self-registers as submodule in agentic-trace-datasets via bootstrap Action, then stays synced via sync-submodule.yml.
